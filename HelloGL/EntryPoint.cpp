#include "Simulation.h"

int main(int argc , char* argv[])
{
	Simulation* simulation = new Simulation(argc , argv); // creates an instance of the game.
	return 0; // assumes a successful exit if our game exists.
}